#include<iostream.h>
#include<conio.h>
void main()
 {
  clrscr();
  char username[50], password[50];
  cout<<"Enter username"<<endl;
  cin>>username;
  cout<<"Enter password"<<endl;
  cin>>password;

  int comparison = username.compare(password);

  if(comparison == 0)
   cout<<"Correct Password"<<endl;
  else
   cout<<"Incorrect Password"<<endl;
  getch();
 }
